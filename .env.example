# ACLED (fatalities) — OAuth via email/password, not a static API key.
# Register a free account at https://acleddata.com/register/
# Locally: copy this file to .env and fill in your own credentials (never commit .env).
# In CI: set these as GitHub repo secrets (Settings -> Secrets and variables -> Actions).
ACLED_EMAIL=
ACLED_PASSWORD=
